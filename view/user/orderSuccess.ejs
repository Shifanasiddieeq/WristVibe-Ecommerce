<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thank You Page</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container my-5">
        <div class="row">

            <div class="col-md-8">
                <h1 class="text-success">Thank You for Your Purchase!</h1>
                <p>Your order has been successfully placed. Below are your order details:</p>


                <div class="mb-4">
                    <h4>Billing Address</h4>
                    <p>
                        <%= order.customer.userName %><br>
                            <%= order.customer.address.houseNo %>, <%= order.customer.address.area %><br>
                                    <%= order.customer.address.town %>, <%= order.customer.address.state %><br>
                                            Pincode: <%= order.customer.address.pincode %><br>
                                                Email: <%= order.customer.email %>
                    </p>
                </div>


                <div>
                    <h4>Order Details</h4>
                    <ul>
                        <% order.products.forEach(product=> { %>
                            <li>
                                <strong>
                                    <%= product.productName %>
                                </strong> - Rs.<%= product.price %> x <%= product.quantity %> = Rs.<%= product.total %>
                            </li>
                            <% }); %>

                                <li>
                                    <strong>Discount:</strong> Rs.<%= appliedCouponDiscount %>
                                </li>
                                <li>
                                    <strong>Shipping:</strong> Rs.<%= shippingFee %>
                                </li>
                                <li>
                                    <strong>Total Price:</strong> Rs.<%= order.totalPrice  %>
                                </li>

                    </ul>


                </div>
                <a href="/home" class="btn-back-home">Back to Home</a>
            </div>



            <div class="col-md-4">


                <div class="card border-success">
                    <div class="card-header bg-success text-white">
                        Order Summary
                    </div>
                    <div class="card-body">
                        <p><strong>Order ID:</strong>
                            <%= order._id %>
                        </p>
                        <p><strong>Order Date:</strong>
                            <%= order.createdAt.toDateString() %>
                        </p>
                        <p><strong>Payment Method:</strong>
                            <%= order.paymentMethod %>
                        </p>
                     
                        <p><strong>Order Total:</strong> Rs.<%= order.totalPrice 
                                %>
                        </p>

                    </div>
                </div>
            </div>
        </div>
    </div>
    <style>
        .btn-back-home {
            display: inline-block;
            padding: 10px 20px;
            color: white;
            background-color: black;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        .btn-back-home:hover {
            background-color: white;
            color: black;
            border: 1px solid black;
        }

        .btn-back-home:active {
            transform: scale(0.95);
        }
    </style>



    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>